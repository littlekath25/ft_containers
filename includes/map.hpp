#pragma once

class map
{
    private:

	public:
		map();
		~map();

		map &operator=(const map &copy);
        // operator[]

		// begin
		// end
		// rbegin
		// rend
		
		// empty
        // size
		// max_size

        // insert
        // erase
        // swap
        // clear

        // key_comp
        // value_comp

        // find
		// count
		// lower_bound
		// upper_bound
        // equal_range

		// get_allocator

	private:

};

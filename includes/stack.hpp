#pragma once

#include <memory>

class stack
{
	private:
	
	public:
		stack();
		~stack();

		// empty
		// size
		// top
		// push
		// pop

		stack &operator=(const stack &copy);
		// operator[]
		// operator==
		// operator!=
		// operator<
		// operator<=
		// operator>
		// operator>=
};

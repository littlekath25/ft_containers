#pragma once

#include <memory>

class vector
{
	private:
	
	public:
		vector();
		~vector();

		// begin
		// end
		// rbegin
		// rend
		
		// size
		// max_size
		// resize
		// capacity
		// empty
		// reserve

		// at
		// front
		// back

		// assign
		// push_back
		// pop_back
		// insert
		// erase
		// swap
		// clear
		
		// get_allocator

		vector &operator=(const vector &copy);
        // operator[]
		// operator[]
		// operator==
		// operator!=
		// operator<
		// operator<=
		// operator>
		// operator>=

		// swap
};
